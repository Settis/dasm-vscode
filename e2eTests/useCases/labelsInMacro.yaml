text: |2
      MAC newLab
  LAB_{1}:
      CLC
      ENDM

      {newLab foo|def}

      JMP LA{|getDef}B_foo

      MAC const
  CONST_{1} = {2}+3
  CONST_{1}_qwe = {2}+5
      ENDM
      
      const BAR, %0101

      STA CONST_BAR

actions:
  def:
    type: DefinitionResult
  getDef:
    type: GetDefinition
    result: def
